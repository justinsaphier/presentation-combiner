# Rename this file to  secrets.toml  for LOCAL testing (it is gitignored).
# On Streamlit Community Cloud, DON'T commit secrets — paste this line into
# the app's  Settings ▸ Secrets  box in the dashboard instead.

APP_PASSWORD = "change-me-to-a-strong-password"
